```markdown
# 角色设定
你是一名资深全栈工程师和开源 AI 模型部署专家，精通前端（React + TypeScript + TailwindCSS）和后端（Python FastAPI + 本地深度学习模型部署）。请帮我从零开始开发一个**完全本地化、零 API 成本**的在线数学公式识别网页应用（类似 SimpleTex）。

---

# 项目简介
本项目是一个在线数学公式 OCR 识别平台。核心特点是**不依赖任何外部付费 API**，所有公式识别均在本地服务器通过开源深度学习模型完成，实现真正的零成本和隐私保护。
用户上传、粘贴或拖拽数学公式图片，系统通过本地模型识别后输出 LaTeX 代码，并提供实时渲染预览、可视化公式编辑、多格式导出等能力。

参考产品：SimpleTex（https://simpletex.cn/）

---

# 技术栈要求（100% 开源 & 本地化）

## 前端
- **框架**：React 18 + TypeScript + Vite
- **样式**：TailwindCSS + shadcn/ui（或 Radix UI）
- **公式渲染**：KaTeX（渲染速度快）
- **代码编辑**：CodeMirror 6（支持 LaTeX 语法高亮）
- **状态管理**：Zustand
- **其他**：React Dropzone（拖拽上传）、html2canvas（导出图片）

## 后端（核心：本地模型推理）
- **框架**：Python 3.10+ + FastAPI
- **本地 OCR 引擎**（必须使用以下开源方案之一，**严禁调用外部网络 API**）：
  - **首选方案：LaTeX-OCR (pix2tex)** 
    - 库：`pix2tex` (基于 ViT 和 ResNet)
    - 优点：专为数学公式设计，体积小，推理快，支持 CPU 和 GPU。
  - **备选方案：Texify 或 UniMERNet** (基于 HuggingFace Transformers)
    - 优点：新一代开源模型，对复杂公式和手写体支持更好。
- **图片处理**：OpenCV / Pillow（本地图片预处理对提升开源模型准确率至关重要）
- **数据库**：SQLite（通过 SQLAlchemy 异步操作，存储历史记录）
- **部署**：Docker（需提供 CPU 版和 NVIDIA GPU 版两种 Dockerfile）

---

# 核心功能需求

## P0 - 核心 MVP（必须实现）
1. **多方式图片输入**：
   - 点击上传（支持 JPG/PNG/WebP，最大 10MB）
   - 全局监听 `Ctrl+V` / `Cmd+V` 粘贴剪贴板截图
   - 拖拽图片到指定区域
2. **本地 AI 识别与代码生成**：
   - 将图片发送至后端，后端使用本地加载的 `pix2tex` 模型进行推理。
   - 返回识别出的 LaTeX 代码。
3. **左右分栏实时预览**：
   - 左侧：CodeMirror LaTeX 编辑器（带行号、语法高亮）
   - 右侧：KaTeX 实时渲染预览（Debounce 300ms）
   - 容错处理：LaTeX 语法错误时，右侧显示红色错误提示而非白屏崩溃。
4. **一键复制与导出**：
   - 一键复制 LaTeX 源码（带 Toast 提示）
   - 导出为 PNG / SVG 图片（基于右侧渲染结果截图）

## P1 - 体验优化（重要）
5. **模型状态监控**：
   - 本地模型首次加载需要时间（几秒到几十秒）。前端需通过轮询获取后端模型加载状态（`loading` / `ready` / `error`），在模型未就绪时禁用上传并显示进度提示。
6. **本地图片预处理（提升识别率的关键）**：
   - 后端自动进行：灰度化、自适应二值化、去除白边、小图自动放大（长边不足 800px 时放大）、倾斜校正。
7. **可视化符号面板**：
   - 提供常用数学符号分类面板（希腊字母、微积分、矩阵等），点击直接插入编辑器。
8. **历史记录**：
   - 自动保存识别记录（原图缩略图转 Base64 存 SQLite + LaTeX 代码 + 时间）。
   - 侧边栏展示历史列表，支持点击回显和删除。

---

# UI/UX 设计要求

## 整体风格
- **风格**：极简、现代、专注效率（类似 Notion 或 SimpleTex）
- **配色**：主色调科技蓝（#2563EB），背景色浅灰/白，文字深灰。
- **布局**：响应式设计，桌面端左右分栏，移动端上下堆叠。

## 页面结构草图
```text
[Logo: LocalMathOCR]      [识别] [编辑器]      [🌙/☀️] [本地模型状态: 🟢 就绪]
--------------------------------------------------------------
                  ✨ 本地智能数学公式识别
         完全免费、保护隐私。截图或拖拽即可生成 LaTeX

  ┌─────────────────────────────────────────────────┐
  │   📷 拖拽图片到此处，或 点击上传 / Ctrl+V 粘贴   │
  │           [ 选择文件 ]   设备: [GPU/CPU ▼]       │
  └─────────────────────────────────────────────────┘

  ┌──────────────────────┐  ┌──────────────────────┐
  │ 📝 LaTeX 源码        │  │ 👁️ 实时预览          │
  │ (CodeMirror Editor)  │  │ (KaTeX Render)       │
  │                      │  │      -b ± √(b²-4ac)  │
  │ \frac{-b \pm \sqrt{}}│  │      ──────────────  │
  │                      │  │           2a         │
  │ [📋复制] [⬇️导出▼]   │  │ [⬇️PNG] [⬇️SVG]      │
  └──────────────────────┘  └──────────────────────┘

  ── 常用符号面板 ──────────────────────────────────
  [α][β][γ][δ]  [±][×][÷][≠]  [∑][∫][∂][∇]  [矩阵]
```

---

# 后端 API 设计

统一响应格式：`{ "code": 200, "message": "success", "data": {...} }`

### 1. 模型状态检查 (前端轮询)
```http
GET /api/model-status
Response Data: { "status": "loading" | "ready" | "error", "device": "cuda" | "cpu", "message": "..." }
```

### 2. 公式识别 (核心本地推理)
```http
POST /api/recognize
Content-Type: multipart/form-data
Params:
- file: 图片文件 (必填)
- preprocess: boolean (是否开启图片预处理，默认 true)

Response Data:
{
  "latex": "\\frac{-b \\pm \\sqrt{b^2 - 4ac}}{2a}",
  "inference_time_ms": 120,
  "preprocessed_image_base64": "data:image/png;base64,..." // 可选：返回预处理后的图片供对比
}
```

### 3. 历史记录 CRUD
```http
GET    /api/history
POST   /api/history
DELETE /api/history/{id}
```

---

# 项目目录结构要求

```text
local-math-ocr/
├── frontend/                    # 前端 (React + Vite)
│   ├── src/
│   │   ├── components/          # UI 组件
│   │   ├── hooks/               # 自定义 Hooks
│   │   ├── services/            # API 请求封装
│   │   ├── stores/              # Zustand 状态
│   │   ├── App.tsx
│   │   └── main.tsx
│   ├── package.json
│   └── Dockerfile               # Nginx 部署前端
│
├── backend/                     # 后端 (FastAPI + 本地模型)
│   ├── app/
│   │   ├── main.py              # 入口，包含启动时异步加载模型的逻辑
│   │   ├── config.py            # 配置 (设备选择、模型路径等)
│   │   ├── routers/             # API 路由
│   │   ├── services/
│   │   │   ├── ocr_engine.py    # 核心：封装 pix2tex 模型的加载与推理
│   │   │   ├── preprocess.py    # 核心：OpenCV/Pillow 图片预处理算法
│   │   │   └── db.py            # SQLite 数据库操作
│   │   └── models/              # Pydantic 数据模型
│   ├── requirements.txt         # 包含 torch, pix2tex, fastapi, opencv-python 等
│   ├── Dockerfile.cpu           # 纯 CPU 环境镜像
│   └── Dockerfile.gpu           # NVIDIA CUDA 环境镜像
│
├── docker-compose.yml           # 一键编排 (提供 CPU 和 GPU 两种 profile)
└── README.md                    # 部署与硬件要求说明
```

---

# 代码质量与特殊要求

1. **模型异步加载**：FastAPI 启动时，必须在后台线程或使用 `lifespan` 异步加载 `pix2tex` 模型，**不能阻塞 API 服务的启动**。在模型加载完成前，`/api/recognize` 应返回 `503 Service Unavailable`。
2. **设备自适应**：后端代码需自动检测是否可用 CUDA，有则用 GPU，无则自动回退到 CPU，并在配置中允许用户强制指定。
3. **内存管理**：如果是 CPU 推理，注意 PyTorch 的内存释放；推理完成后及时清理显存/内存缓存。
4. **完整性**：请给出所有核心文件的完整代码，**绝对不要**使用 `// ... 省略` 跳过代码。如果超出字数限制，请在末尾标注 `【未完待续，请回复"继续"】`。
5. **执行步骤**：
   - 第一步：输出 `docker-compose.yml` (含 CPU/GPU profile) 和 `README.md` (含本地硬件要求说明)。
   - 第二步：输出后端 `backend/` 的所有核心代码（重点是 `ocr_engine.py` 和 `preprocess.py`）。
   - 第三步：输出前端 `frontend/` 的所有核心代码。
   